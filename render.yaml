services:
  - type: web
    name: automl-agent
    env: docker
    region: singapore
    plan: free
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
      - key: OLLAMA_URL
        value: ""

